const routes = createRoutes({ list: "{{kebabCase (pluralize resource)}}", add: "{{kebabCase
	(pluralize resource)
}}-form", edit: "{{kebabCase (pluralize resource)}}-id-edit", details: "{{kebabCase
	(pluralize resource)
}}-id", }); export default routes;