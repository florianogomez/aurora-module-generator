
import {
{{pascalCase resource}}Store } from "../index"; import {
{{pascalCase resource}}Interface } from "../../interfaces/{{kebabCase resource}}_interface"; /** *
remove action * * @param
{{"{"}}{{pascalCase resource}}Store} store - The Pinia store instance * @param
{{"{"}}
id:
{{pascalCase resource}}Interface["id"] }} payload - ID to remove * @returns {void} */ export
function remove(store:
{{pascalCase resource}}Store, { id }: { id:
{{pascalCase resource}}Interface["id"] }) { store.elements = store.elements.filter((e) => e.id !==
id); }